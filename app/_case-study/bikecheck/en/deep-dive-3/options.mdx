{/* Placeholder prose. The real copy is ticket 14. */}

- **Native Kotlin** — the best result on the device, and a second language, a second toolchain and no shared code with the API.
- **React Native** — genuinely native rendering, at the price of a distinct component ecosystem from the one the rest of the stack uses.
- **Capacitor** — the existing React application in a native shell, with plugin access to the device where it is needed.
