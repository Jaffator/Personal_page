{/* Placeholder prose. The real copy is ticket 14. */}

Capacitor. The application is mostly forms, lists and history — screens a web view
renders perfectly well — and the parts that genuinely need the device, storage and
haptics, are plugins rather than the whole architecture.

TypeScript then runs from the database client to the interface, and a type shared
between the API and the application is shared rather than copied.
