{/* Placeholder prose. The real copy is ticket 14. */}

- **Full offline replica** — mirror the schema on the device and reconcile later. Correct in every case, and it puts the wear rules in two languages on two runtimes.
- **Block writes when offline** — honest and simple, and it fails at the exact moment the application is most useful.
- **Queue the ride, derive on sync** — accept the ride locally as a fact, and let the server remain the only thing that decides what it means.
