{/* Placeholder prose. The real copy is ticket 14. */}

Queue the ride and derive on sync. A ride is an observation — a distance, a
duration, a bicycle, a timestamp — and observations do not conflict. Wear is an
interpretation of observations, and interpretation stays in one place.

The queue is therefore append-only, and syncing is replaying it in order.
