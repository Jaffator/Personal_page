{/* Placeholder prose. The real copy is ticket 14. */}

A rider finishes a ride somewhere with no signal and wants to log it there, not
later. So writes have to succeed with no network — but wear is derived on the
server, and a client that computes its own wear while offline is a second
implementation of the rule that decides when a chain needs replacing.
