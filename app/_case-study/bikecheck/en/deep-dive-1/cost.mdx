{/* Placeholder prose. The real copy is ticket 14. */}

Wear figures are stale while offline, and the interface has to say so rather than
show a confident number that is wrong. That is a visible compromise a Reader can
see in the application.

It also handles a batch of queued rides less gracefully than it handles one: they
replay correctly, but the rider watches a progress state rather than seeing the
result immediately. That is the trade named in the retrospective.
