{/* Placeholder prose. The real copy is ticket 14. */}

Every wear figure is a join and an aggregate rather than a column read, so the
component list was the first screen that needed indexing work to stay quick.

It also makes bad data harder to enter and harder to fix: overlapping fitments
have to be rejected, which is more validation than a counter would have needed.
The schema is right, and it is not the smallest thing that could have worked.
