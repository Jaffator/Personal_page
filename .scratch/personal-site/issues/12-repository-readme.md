# 12 — Repository README

**What to build:** This repository is public and is itself a second work sample — a Reader who likes the site will look for its source. The README should reward that.

It explains what the site is, how it is structured, how to run and verify it, and — briefly — the decisions that would otherwise look odd, pointing at the ADRs rather than restating them. Next.js for a small static site, MDX per locale rather than translation keys, and asymmetric locale URLs all look like mistakes without their context.

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] The README states what the site is and who it is for
- [ ] Running it locally and running the verification suite are both documented and correct
- [ ] The content model is explained well enough that adding a Project is obvious
- [ ] The three recorded decisions are referenced, pointing at the ADRs rather than duplicating them
- [ ] No secrets, credentials or personal data beyond what the site already publishes
- [ ] Prose is in English and free of placeholder or generated-scaffold text
